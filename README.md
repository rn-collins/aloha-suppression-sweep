# Platform Moderation Evidence Lab

A source-linked register that separates case decisions, official inquiries, platform statements, and advocacy allegations. Every record states both what its source establishes and its evidentiary boundary.

## Verify locally

```sh
npm test
npm run build
```

The application has no account, analytics, or submission endpoint. Browser-local review state uses the key `platform-evidence-lab-v1`.
